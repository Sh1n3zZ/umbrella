version: "2"
sql:
  - engine: "postgresql"
    schema: "db/migration/sql" # Folder with .sql files (only UPs are parsed)
    queries: "internal/sqlc/query" # Folder with SELECT/INSERT/UPDATE SQL
    gen:
      go:
        package: "sqlc"
        out: "internal/sqlc"
        sql_package: "pgx/v5"
